{
  :id => user.id,
  :first_name => user.first_name,
  :last_name => user.last_name,
  :username => user.username,
  :email => user.email,
  :password_digest => user.password_digest,
  :trips_owned => user.trips_owned,
  :trips_collaborated => user.trips_collaborated,
}
